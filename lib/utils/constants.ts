/** Platform-wide currency code. Change here to affect all UI. */
export const PLATFORM_CURRENCY = "TND";

/** Canonical base URL for the site. Set NEXT_PUBLIC_SITE_URL in .env for production. */
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://guidni.com";
